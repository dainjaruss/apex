# Supabase (required at runtime; build uses a placeholder if unset)
NEXT_PUBLIC_SUPABASE_URL=https://xbnwxiziqhcaxvztewqe.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — never expose to the browser or commit real values
SUPABASE_SERVICE_ROLE_KEY=

# E2E testing
E2E_BASE_URL=http://localhost:3099
E2E_PORT=3099
# Shared password for all @franklyn.dev seed users (scripts/seed-e2e.ts)
E2E_TEST_PASSWORD=
